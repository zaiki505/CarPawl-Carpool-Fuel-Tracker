/**
 * Round purple scroll-to-top button with glow shadow and bouncy press.
 */
export interface ScrollTopButtonProps {
  /** Defaults to smooth-scrolling the window to top */
  onClick?: (e: any) => void;
  style?: any;
}
