Purple-tinted pill breadcrumb shown at the top of subpages.

```jsx
<Breadcrumb items={[{ label: "Home", href: "index.html" }]} current="Projects" />
```

Renders `Home / Projects` inside a 999px pill with `rgba(167,84,255,0.07)` bg. Current page is bold purple.
